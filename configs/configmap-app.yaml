apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: app
data:
  APP_ENV: "production"
  APP_PORT: "80"
  app.properties: |
    env=production
    port=80
    greeting=hello from a config file